# -*- coding: utf-8 -*-
"""
추천 가중치 (학부 캡스톤용 휴리스틱).

이 수치는 임의 튜닝 상수가 아니라, 같은 날 여행 결정 흐름을 단순화한 것이다:
  "오늘 갈 수 있나?" → 날씨
  "내 목적에 맞나?" → 목적
  "당일 동선에 현실적인가?" → 거리
  "지금 시간대에 의미 있나?" → 시간
  "계절·이벤트 가산?" → 보너스

과학적 최적화를 주장하지 않으며, 대표 시나리오로 자연스러운지 검토하는 수준이다.
"""

from __future__ import annotations

# ── A. 관광지(메인) 추천: 합계 100 ───────────────────────────────
MAIN_WEIGHTS: dict[str, int] = {
    "weather_fit": 35,       # 날씨·대기 맥락이 가장 큰 제약 (프로젝트 정체성)
    "goal_fit": 25,        # 힐링/사진/실내 등 사용자 목적 부합
    "distance_fit": 20,    # 당일 이동 현실성
    "time_fit": 10,        # 시간대·일정 길이 보조
    "season_event_bonus": 10,  # 골든아워·태그 기반 보너스 (핵심 결정 아님)
}

# ── B. 다음 코스(식당·카페): 합계 100 ───────────────────────────
NEXT_COURSE_WEIGHTS: dict[str, int] = {
    "distance_fit": 30,    # 직전 장소에서 이어지기 쉬운가
    "stage_fit": 25,     # 식사/티타임 등 단계에 맞는가
    "quality_fit": 20,   # 평점+리뷰 수 기반 신뢰 (원점수만 쓰지 않음)
    "goal_fit": 15,      # 동행·목적과의 약한 정합
    "weather_fit": 10,   # 비·미세먼지 시 실내 성향 등 보조
}
